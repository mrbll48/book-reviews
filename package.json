{
  "name": "book-reviews",
  "description": "This is a book review site",
  "author": "mrbll48",
  "version": "0.0.1",
  "main": "server.js",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.1",
    "prettier": "^3.0.3"
  },
  "scripts": {
    "start": "node server/server.jsx",
    "dev": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "build": "cd client && npm run build"
  }
}
